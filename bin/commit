#!/usr/bin/env bash

message=$1
branch=$2
remote=$3

if [$remote == ""]; then
	echo test
fi

echo adding changes ...
git add .
echo commiting changes ...
git commit -m "automated commit"
echo pushing changes ...
git push origin master
