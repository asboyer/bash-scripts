#!/usr/bin/env bash
input="/home/$USER/tasks.txt"

clear

echo
echo Hello, $USER!
echo
echo
cal
if [ -f "$input" ]; then
	echo Today\'s Tasks:
	echo "------------------"
    while IFS= read -r line
	do
		echo "$line"
	done < "$input"
	echo "------------------"
	echo
else 
	echo
fi
